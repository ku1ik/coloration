#!/usr/bin/env ruby

lib_dir = File.dirname(__FILE__) + '/../lib'
$LOAD_PATH.unshift(lib_dir) unless $LOAD_PATH.include?(lib_dir)

require 'coloration'

argv = ARGV.dup
Coloration::CLIParser.process(Coloration::Converters::TextMate::JEdit.new, argv)
